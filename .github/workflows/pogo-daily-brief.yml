name: Pokémon GO Daily Ops Briefing

on:
  schedule:
    - cron: '0 0 * * *' # 매일 00:00 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-dashboard:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ▼▼▼ [수정 1] 스크립트 실행 전에 원격 변경사항을 먼저 당겨옵니다 (충돌 방지) ▼▼▼
      - name: 🔄 Pull Latest Changes
        run: |
          git config --global user.name "Grand-Ops-Master-Bot"
          git config --global user.email "actions@github.com"
          git pull --rebase origin main || echo "Remote is fresh, proceeding."

      - name: 🐍 Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 📦 Install Dependencies
        run: |
          pip install -r requirements.txt

      # ▼▼▼ [수정 2] 이제 안전하게 파일을 생성합니다 ▼▼▼
      - name: 🔄 Generate Dashboard
        run: |
          python scripts/generate_dashboard.py

      - name: 💾 Commit and Push Changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs: Update POGO Dashboard & Log History [skip ci]"
          file_pattern: "POGO_DAILY_BRIEF.md"
          commit_user_name: "Grand-Ops-Master-Bot"
          commit_user_email: "actions@github.com"
