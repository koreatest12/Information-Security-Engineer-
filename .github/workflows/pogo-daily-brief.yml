name: Pokémon GO Daily Ops Briefing

on:
  schedule:
    - cron: '0 0 * * *' # 매일 00:00 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-dashboard:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      # ▼▼▼ [강력한 에러 해결] 원격 상태로 강제 초기화 ▼▼▼
      - name: 🔄 Hard Reset to Remote
        run: |
          git fetch origin main
          git reset --hard origin/main
          echo "✅ Local branch reset to match origin/main exactly."

      - name: 🐍 Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 📦 Install Dependencies
        run: |
          pip install -r requirements.txt

      # ▼▼▼ 스크립트 실행 (README.md 갱신 + Actions 화면 출력) ▼▼▼
      - name: 🔄 Generate Dashboard
        run: |
          python scripts/generate_dashboard.py

      # ▼▼▼ [강제 푸시] 에러 무시하고 덮어쓰기 ▼▼▼
      - name: 💾 Commit and Push Changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs: Update POGO Dashboard (Force Sync) [skip ci]"
          file_pattern: "README.md"  # 메인 대시보드 파일
          commit_user_name: "Grand-Ops-Master-Bot"
          commit_user_email: "actions@github.com"
          push_options: '--force'
