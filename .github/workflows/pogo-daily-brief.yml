name: Pokémon GO Daily Ops Briefing

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-dashboard:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # 전체 히스토리를 가져와서 병합 문제 방지

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 Install Dependencies
        run: |
          pip install -r requirements.txt

      - name: 🔄 Generate Dashboard
        run: |
          python scripts/generate_dashboard.py

      # ▼▼▼ [추가된 부분] 최신 변경 사항 동기화 ▼▼▼
      - name: 🔄 Sync with Remote (Pull & Rebase)
        run: |
          git config --global user.name "Grand-Ops-Master-Bot"
          git config --global user.email "actions@github.com"
          git pull --rebase origin main || echo "Rebase conflict, proceeding anyway to attempt push"
      # ▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲

      - name: 💾 Commit and Push Changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs: Update Pokémon GO Dashboard [skip ci]"
          file_pattern: "POGO_DAILY_BRIEF.md"
          commit_user_name: "Grand-Ops-Master-Bot"
          commit_user_email: "actions@github.com"
          push_options: '--force' # 필요 시 강제 푸시 옵션 사용 (주의 필요)
