name: Pokémon GO Ops Server & Release

on:
  schedule:
    - cron: '0 0 * * *' # 매일 00:00 UTC
  workflow_dispatch:

permissions:
  contents: write # 리포지토리 수정 및 릴리즈 생성 권한

jobs:
  ops-server-deployment:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Initialize Server (Checkout)
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      # [강제 동기화] 충돌 방지
      - name: 🔄 Sync Repository State
        run: |
          git fetch origin main
          git reset --hard origin/main

      # [업그레이드] Python 3.12 적용
      - name: 🐍 Upgrade Python Engine to 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 📦 Install Server Dependencies
        run: |
          pip install -r requirements.txt

      # [서버 가동] 데이터 수집 -> DB 저장 -> README 갱신
      - name: 🚀 Run Ops Server
        run: |
          python scripts/ops_server.py

      # [커밋] 변경된 DB 및 README 저장
      - name: 💾 Snapshot Data
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore(ops): Daily Server Sync & DB Update [skip ci]"
          file_pattern: "README.md data/*.json"
          commit_user_name: "Grand-Ops-Master-Bot"
          commit_user_email: "actions@github.com"
          push_options: '--force'
          tagging_message: "v${{ steps.date.outputs.date }}" # 태그 준비 (아래 단계에서 생성)

      # [릴리즈] 버전 태그 생성 및 배포
      - name: 🏷️ Get Date for Release Tag
        id: date
        run: echo "date=$(date +'%Y.%m.%d')" >> $GITHUB_OUTPUT

      - name: 🚀 Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.date.outputs.date }}
          name: 📢 Ops Report v${{ steps.date.outputs.date }}
          body: |
            ## 📝 Daily Operations Log
            자동화 서버가 수집한 오늘의 포켓몬고 인텔리전스 데이터입니다.
            
            - **Engine:** Python 3.12
            - **Status:** Stable
            
            ### 📂 Artifacts
            - `daily_intel.json`: 수집된 원본 데이터베이스
          files: |
            data/daily_intel.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
