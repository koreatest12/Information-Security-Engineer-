name: 🤖 Security Material Auto-Collector

on:
  schedule:
    - cron: '0 0 * * 1' # 매주 월요일 자정 실행
  workflow_dispatch:      # 수동 실행 버튼

permissions:
  contents: write

jobs:
  collect-and-update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install requests

      - name: Run Collection Script
        run: |
          python scripts/fetch_security_news.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name "SecurityBot"
          git config --global user.email "bot@noreply.github.com"
          
          # materials 폴더와 README.md 변경사항 확인
          git add materials/ README.md
          
          # 변경사항이 있을 때만 커밋
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "docs: auto-update security materials form KISA/OWASP"
            git push
          fi
